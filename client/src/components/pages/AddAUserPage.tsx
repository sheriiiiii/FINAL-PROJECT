import React from 'react';

const AddUserPage: React.FC = () => {
  return (
    <div className="p-8">
      <h1 className="text-4xl font-bold">Add a User</h1>
      {/* Form for adding a new user */}
    </div>
  );
};

export default AddUserPage;
